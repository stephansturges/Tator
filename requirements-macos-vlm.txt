# MLX-VLM install target for the macOS backend.
#
# In the full .venv-macos environment, install this file with --no-deps after
# requirements-macos-inference.txt. mlx-vlm 0.3.9 is the newest release that
# includes Qwen3-VL support while staying on Transformers 4.57; newer releases
# require Transformers 5.x, and its OpenCV metadata conflicts with SAHI 0.11.x.

mlx>=0.31,<1.0
mlx-vlm==0.3.9
